#Pour creer un environnement virtuel fair la commande suivante: 
# "python3.10 -m venv env"

#Pour lancer l'environnement virtuel faire la commande suivante:
# "source env/bin/activate"
#Toujours travailler depuis cet environnement virtuel pour eviter
# les conflits de versions

#Pour installer Django faire la commande suivante:
# "pip install django"

#Pour creer un projet Django faire la commande suivante:
# "django-admin startproject nom_projet"

#Pour lancer le serveur faire la commande suivante:
# "python manage.py runserver"
#Le fichier manage.py est un fichier qui permet de lancer des commandes
# pour gérer notre projet Django sans passer par django-admin

#Pour eviter les erreurs de migration faire la commande suivante:
# "python manage.py migrate"
# Cela sert a appliquer les migrations c est a dire a creer les tables
# de la base de donnees a partir des modeles de notre application Django

#Bootstrap est un framework CSS qui permet de creer des pages web
# avec 12 colonnes c est a dire que l on peut diviser une page en 12
# colonnes et placer des elements dans ces colonnes. Bootstrap permet
# de creer des pages web responsive c est a dire qui s adaptent a la taille
# de l ecran de l utilisateur. Pour utiliser Bootstrap il faut ajouter
# les fichiers CSS et JS de Bootstrap dans le dossier static de notre projet
# Il y a 4 types de colonnes dans Bootstrap: xs, sm, md, lg en fonctiond de la taille
# de l ecran de l utilisateur. Aller directement sur le site de Bootstrap pour
# plus obtenir l ensemble des classes et des composants disponibles.